# IMPORTANT: Never commit this file with real secrets to git!

# --- Environment ---
NODE_ENV=production
PORT=5004

# --- App URL (used for password reset links, etc.) ---
APP_URL=https://applyn.co.in

# --- Database / Storage ---
DATABASE_URL=mysql://applyn_admin:Applyn%23Admin2026%21Strong@localhost:3306/applyn-db
STORAGE=mysql
SESSION_STORE=mysql
SESSION_SECRET=dev-secret-change-me
DB_POOL_SIZE=10

# --- CSRF / Origins ---
APP_ORIGINS=https://applyn.co.in,https://www.applyn.co.in

# --- Artifacts / Build worker ---
ARTIFACTS_DIR=/var/applyn/artifacts
ANDROID_BUILDER_IMAGE=applyn-android-builder:latest
WORKER_ID=
INLINE_WORKER=true
WORKER_POLL_MS=2000
BUILD_TIMEOUT_MS=1200000
MAX_BUILD_ATTEMPTS=3
BUILD_JOB_LOCK_TTL_MS=1800000
BUILD_RETRY_BACKOFF_MS=10000
ARTIFACT_RETENTION_DAYS=30
ARTIFACT_MAX_PER_APP=10

# --- Mock build (for testing) ---
MOCK_ANDROID_BUILD=false
MOCK_ANDROID_BUILD_FAIL_ONCE=false

# --- Google OAuth ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://applyn.co.in/api/auth/google/callback

# --- Razorpay Payment Gateway ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# --- Push Notifications (OneSignal) ---
ONESIGNAL_APP_ID=
ONESIGNAL_API_KEY=

# --- iOS Build Configuration (GitHub Actions) ---
# Your GitHub username or organization
GITHUB_OWNER=
# Repository name where this code is pushed
GITHUB_REPO=
# GitHub Personal Access Token with 'repo' and 'workflow' permissions
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=
# URL where GitHub will POST build completion status
# Example: https://applyn.co.in/api/ios-build-callback
IOS_BUILD_CALLBACK_URL=
# Secret token for iOS callback authentication (generate a random string)
# Also add this to GitHub repo secrets as IOS_CALLBACK_SECRET
IOS_CALLBACK_SECRET=

# --- SMTP Email Configuration ---
# For password reset and other transactional emails
# If not configured, emails will be logged to console
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@applyn.co.in
SMTP_SECURE=false

# --- AI / LLM Configuration ---
# Choose provider: "claude" (default) or "openai"
LLM_PROVIDER=claude
# Anthropic Claude API Key (required if LLM_PROVIDER=claude)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=
# OpenAI API Key (required if LLM_PROVIDER=openai)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# --- E2E sanity script ---
BASE_URL=https://applyn.co.in
SANITY_ORIGIN=https://applyn.co.in
SANITY_REFERER=https://applyn.co.in/
SANITY_TIMEOUT_MS=180000
