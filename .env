# Local dev defaults (safe placeholders)
# Replace values before using on a VPS.

NODE_ENV=development
PORT=5000

DATABASE_URL=mysql://root:password@localhost:3306/saas_architect
STORAGE=mysql
SESSION_STORE=mysql
SESSION_SECRET=dev-secret-change-me
DB_POOL_SIZE=10
